single(count=6)
	->set(name='Joe'+[@index],BankAccount/balance=ToDouble([@index]))
	->log()
	->sqlinsert(resource='sportlinkkernel'
		,query='INSERT INTO TESTEXAMPLE VALUES (?,?)'
		,[name]
		,[BankAccount/balance]
	)
	->flattenmsg()
	->tml(messageName='Inserted', isArray=true)