single()
	->set(SearchName='')
	->tmlstream(messageName='MessageName', isArray=false)
	->call(service='some/Service',debug=false)
	->streamtoimmutable(path='/ToplevelResult')
	->take(5)
	->tmlstream(messageName='SecondInput', isArray=false)
	->call(service='DetailService',debug=false)
	->inmessage(name='MessageName',isArrayElement=true)
	->flatten()
		
