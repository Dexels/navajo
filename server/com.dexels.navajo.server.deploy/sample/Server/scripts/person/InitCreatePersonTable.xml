<?xml version="1.0" encoding="UTF-8"?>
<tsl author="Matthijs Philip" id="person/ProcessInsertPerson" notes="" repository="$Id$" xmlns="http://www.dexels.com/navascript" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.dexels.com/navascript http://www.dexels.com/schema/navascript.xsd">
    <param name="UpdateCount">
        <expression value="0"/>
    </param>
    <map object="com.dexels.navajo.adapter.SQLMap">
        <field name="datasource">
            <expression value="'default'"/>
        </field>
        <field name="update">
            <expression xml:space="preserve">
		  			CREATE TABLE person (
		  				id int  NOT NULL AUTO_INCREMENT, -- this is mysql
		  			--	id int  GENERATED BY DEFAULT AS IDENTITY , this is derby
		  				lastname varchar(64),
		  				infix varchar(16),
		  				initials varchar(8),
						firstname varchar(64), 
						dateofbirth date, 
						nationality varchar(16),  
		  			    sex  smallint,
		  			    streetname varchar(64),
						housenumber int, 
						numberappendix varchar(8),
						city varchar(64), 
						zipcode varchar(8), 
						countrycode varchar(16), 
						telephonenumber varchar(16), 
						mobilenumber varchar(16),
		  			    emailaddress varchar(64),
		  			    PRIMARY KEY (id)
		  		  )
            </expression>
        </field>
  
        <field name="doUpdate">
            <expression value="true"/>
        </field>
        <param name="UpdateCount">
            <expression value="[/@UpdateCount] + $updateCount"/>
        </param>
        <!-- for the photo get person id -->
       
    </map>
   
</tsl>
