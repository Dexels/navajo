<?xml version="1.0" encoding="UTF-8" ?>
<!-- $Id$ -->
<project name="NavajoDocument" default="archive" basedir=".">
	<property name="tar.archive" value="NavajoServer.tgz" />
	<property name="zip.archive" value="NavajoServer.zip" />

	<target name="tar">
		<tar tarfile="${tar.archive}" basedir="." excludes="NavajoServer.tgz,NavajoServer.zip,compiled,classes,build.xml">

		</tar>
	</target>
	<target name="zip">
		<zip zipfile="${zip.archive}" basedir="."  excludes="NavajoServer.tgz,NavajoServer.zip,compiled,classes,build.xml">
		</zip>
	</target>
	<target name="archive" depends="tar,zip" />
	<target name="publish">
		<scp file="" todir=""></scp>
	</target>
</project>
<!-- EOF: $RCSfile$ -->
